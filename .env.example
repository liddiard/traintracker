# Database (path differs between local and Docker)
# Local dev: file:./db/app.db
# Docker:    file:/app/db/app.db
DATABASE_URL=file:./db/app.db

# Web Push VAPID keys (generate with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:your-email@example.com

# Enable push notification background polling (optional, defaults to false)
ENABLE_NOTIFICATIONS=false

# Docker deployment: host port mapped to container (optional, defaults to 8724)
PORT=8724
